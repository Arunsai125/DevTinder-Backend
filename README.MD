Hey This is the README file of this project, We would be adding the content here as soon as we build this project
Initially, we have installed express to perform the server connection and few CRUD operations, we also made few API testings for the functions that we wrote.
We have installed nodemon to enable the hot reload feature for Node.js
